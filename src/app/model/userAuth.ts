export interface LoginUserData {
  username: string;
  password: string;
}

export interface RegisterUserData {
  firstName: string;
  lastName: string;
  userType: string;
  email: string;
  password: string;
}
